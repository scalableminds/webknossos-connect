#!/bin/bash

docker build --target production -t py-datastore .
(sleep 3; curl http://localhost:8000/api/neuroglancer/Connectomics_Department/test -X POST -H "Content-Type: application/json" --data-binary "@neuroglancer.json") &
docker run --rm -it --network host py-datastore "$@"
